---
import "../styles/global.css";
---
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <meta name="description" content="Building Africa's future in data, infrastructure, and intelligence." />
    <meta name="keywords" content="Africa, data, infrastructure, intelligence, newsletter, technology" />
    <meta property="og:title" content="The African Stack" />
    <meta property="og:description" content="Building Africa's future in data, infrastructure, and intelligence." />
    <meta property="og:type" content="website" />
    <title>{Astro.props.title || "The African Stack"}</title>
    
    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    
    <!-- Import global styles -->
    <link rel="stylesheet" href="../styles/global.css">
  </head>
  <body class="font-inter antialiased">
    <nav class="fixed top-0 w-full bg-white/95 backdrop-blur-md border-b border-gray-200 z-50 transition-all duration-300">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between items-center h-16">
          <a href="/" class="text-2xl font-bold bg-gradient-to-r from-indigo-600 to-purple-600 bg-clip-text text-transparent">
            The African Stack
          </a>
          <div class="hidden md:flex space-x-8">
            <a href="/" class="text-gray-600 hover:text-indigo-600 font-medium transition-colors duration-300">Home</a>
            <a href="/archive" class="text-gray-600 hover:text-indigo-600 font-medium transition-colors duration-300">Archive</a>
            <a href="/about" class="text-gray-600 hover:text-indigo-600 font-medium transition-colors duration-300">About</a>
          </div>
        </div>
      </div>
    </nav>
    
    <main class="pt-16 min-h-screen">
      <slot />
    </main>
    
    <footer class="bg-gray-900 text-white py-12 mt-16">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-8 mb-8">
          <div>
            <h3 class="text-xl font-bold mb-4">The African Stack</h3>
            <p class="text-gray-400">Building Africa's future in data, infrastructure, and intelligence.</p>
          </div>
          <div>
            <h4 class="text-lg font-semibold mb-4">Subscribe</h4>
            <form class="flex gap-2" id="footer-signup">
              <input 
                type="email" 
                placeholder="Enter your email" 
                required
                class="flex-1 px-3 py-2 bg-gray-800 border border-gray-700 rounded-lg text-white placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-indigo-500"
              >
              <button 
                type="submit" 
                class="px-6 py-2 bg-gradient-to-r from-indigo-600 to-purple-600 text-white rounded-lg font-medium hover:transform hover:-translate-y-0.5 transition-all duration-200"
              >
                Subscribe
              </button>
            </form>
          </div>
        </div>
        <div class="text-center pt-8 border-t border-gray-800">
          <p class="text-gray-400">&copy; 2024 The African Stack. All rights reserved.</p>
        </div>
      </div>
    </footer>
  </body>
</html>

<style>
  .font-inter {
    font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
  }
</style>
